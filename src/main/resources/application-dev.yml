server:
  port: 8086
  address: 0.0.0.0

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sports-admin?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: sports-api-secret-key-2024-please-change-in-production-environment
  expiration: 604800000

# 百度归因
baidu:
  apptrack:
    app-id: 22870
    app-secret: ${BAIDU_APPTRACK_SECRET:dev-app-secret-placeholder}
